import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SHeaderText = ({title}) => {
  return <Text className="text-white text-lg font-bold">{title}</Text>;
};

export default SHeaderText;

const styles = StyleSheet.create({});
